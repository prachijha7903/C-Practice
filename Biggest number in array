#include<stdio.h>
int main(){
    int arr[6] = {1,4,5,9,6,3};
    int max = arr[0];
    for(int i=0;i<=5;i++){
        if(max<arr[i])
        max = arr[i];
    }
    printf("%d",max);
    return 0;
}
