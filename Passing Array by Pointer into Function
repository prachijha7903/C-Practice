#include<stdio.h>

void Printarray(int *ptr);

int main(){
    int arr[]={1,2,3,4,5};
    int *ptr = &arr[0];
    Printarray(arr);
    return 0;
}
void Printarray(int *ptr){
    for(int i=0;i<5;i++){
        printf("The value of array at %d index is %d\n",i,ptr[i]);
    }
    
}
