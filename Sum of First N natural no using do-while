#include<stdio.h>
int main(){
    int n,sum;
    printf("ENter the number: ");
    scanf("%d",&n);

    sum=0;
    int i=1;
    do{
        sum=sum+i;
        i++;
    }while(i<=n);
    printf("The sum of first %d natural number will be %d",n,sum);

    return 0;
}
