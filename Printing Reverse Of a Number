#include<stdio.h>
int main(){
    int n,i;
    printf("Enter no: ");
    scanf("%d",&n);
    int ld;
    int r=0;
    while(n>0){
         r = r*10;
         ld = n%10;
         r = r + ld;
         n=n/10;
    }
    printf("The reverse of the number will be: %d",r);

return 0;
}
