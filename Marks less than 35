#include <stdio.h>
int main()
{
    int marks[10] = {88, 98, 32, 12, 56, 76, 99, 100, 19, 73};
    for (int i = 0; i <= 9; i++)
    {
        if (marks[i] < 35)
            printf("%d ", i);
    }
    return 0;
}
