#include<stdio.h>
#include<math.h>

int main() {
    int a,b;
    printf("ENter the Numbers:");
    scanf("%d %d",&a,&b);

    int power = pow(a,b);
    printf("The value of %d to the power is %d is %d",a,b,power);

}
